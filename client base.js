class client base {
    constructor(settings) {
        this.__settings = Object.assign(VideoPlayer.getDefaultSettings(), settings);
        this.__interfase = null;
        this.__buttons = null;
        this.__wallpapers = null;
        
            }
            
return {
            inter: '',
            videoContainer: '.mplayer',
            volume: 2,
        };
    }
